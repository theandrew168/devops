---
# version of victoria metrics to install
victoria_metrics_version: v1.149.0

# directory to hold victoria metrics data
# NOTE: don't use a mount point directly: use a subdir
victoria_metrics_data_dir: ~

# version of victoria logs to install
victoria_logs_version: v1.52.0

# directory to hold victoria logs data
# NOTE: don't use a mount point directly: use a subdir
victoria_logs_data_dir: ~

# version of victoria traces to install
victoria_traces_version: v0.10.0

# directory to hold victoria traces data
# NOTE: don't use a mount point directly: use a subdir
victoria_traces_data_dir: ~
